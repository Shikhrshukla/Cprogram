#include<stdio.h>

int main(){
    int x, y, Sum;
    printf("Enter x: ");
    scanf("%d", &x);
    printf("Enter y: ");
    scanf("%d", &y);

    Sum = x + y;
    printf("Sum is: %d", Sum);

    return 0;
}